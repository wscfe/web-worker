export enum ETransferableType {
  auto = 'auto',
  none = 'none',
}

export enum EWorkerEndType {
  success = 'success',
  error = 'error',
  timeout_expired = 'timeout_expired',
  killed = 'killed',
}
